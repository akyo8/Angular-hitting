/* tslint:disable:no-unused-variable */

/*
import {
  beforeEach, beforeEachProviders,
  describe, xdescribe,
  expect, it, xit,
  async, inject
} from '@angular/core/testing';
import { SimpleSearchService } from './simple-search.service';

describe('SimpleSearch Service', () => {
  beforeEachProviders(() => [SimpleSearchService]);

  it('should ...',
      inject([SimpleSearchService], (service: SimpleSearchService) => {
    expect(service).toBeTruthy();
  }));
});
*/
